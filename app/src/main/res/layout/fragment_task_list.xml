<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_top">

    <!-- Task list page -->
    <!-- Shows the task list and the add-task button -->
    <!-- Main content stack -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Screen title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="20dp"
            android:text="@string/task_list_title"
            android:textColor="@color/dark_text"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- White content card -->
        <!-- Scrolls when the task list gets long -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_card_white"
            android:elevation="2dp"
            android:fillViewport="true">

            <!-- Page content -->
            <!-- Holds the empty state and the generated task rows -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <!-- Empty state -->
                <!-- Shown when there are no tasks -->
                <TextView
                    android:id="@+id/tvEmptyTasks"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:layout_marginTop="40dp"
                    android:text="@string/empty_tasks_message"
                    android:textColor="@color/secondary_text"
                    android:textSize="15sp"
                    android:visibility="gone" />

                <!-- Task list container -->
                <!-- Task rows are added here at runtime -->
                <LinearLayout
                    android:id="@+id/taskContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />

            </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <!-- Add-task button -->
    <!-- Floating action button -->
    <FrameLayout
        android:id="@+id/fab_add_task"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:background="@drawable/bg_fab_green"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:elevation="6dp"
        android:focusable="true">

        <!-- Plus icon -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:contentDescription="@string/create_new_task"
            android:src="@drawable/ic_add"
            app:tint="@color/white" />
    </FrameLayout>

</FrameLayout>
