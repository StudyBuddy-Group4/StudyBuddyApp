<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Camera & Microphone -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Bluetooth (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Foreground service for ongoing calls -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyBuddyApp"
        tools:targetApi="31">

        <!-- Splash (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Flow -->
        <activity android:name=".LaunchOptionsActivity" android:exported="false" />
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".CreateAccountActivity" android:exported="false" />
        <activity android:name=".ForgotPasswordActivity" android:exported="false" />
        <activity android:name=".SecurityCodeActivity" android:exported="false" />
        <activity android:name=".NewPasswordActivity" android:exported="false" />
        <activity android:name=".PasswordChangedActivity" android:exported="false" />

        <!-- Main Hub -->
        <activity android:name=".MainHubActivity" android:exported="false" />

        <!-- Meeting Room -->
        <activity
            android:name=".MeetingRoomActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboard|keyboardHidden|screenLayout"
            android:keepScreenOn="true" />
        <activity android:name=".FlagParticipantActivity" android:exported="false" />
        <activity android:name=".AdminMeetingRoomActivity" android:exported="false" />

        <!-- Profile Sub-screens -->
        <activity android:name=".EditProfileActivity" android:exported="false" />
        <activity android:name=".SecurityMenuActivity" android:exported="false" />
        <activity android:name=".ChangePasswordActivity" android:exported="false" />
        <activity android:name=".PasswordChangedSuccessActivity" android:exported="false" />
        <activity android:name=".SettingsMenuActivity" android:exported="false" />
        <activity android:name=".NotificationSettingsActivity" android:exported="false" />
        <activity android:name=".DeleteAccountActivity" android:exported="false" />
        <activity android:name=".HistoryReportsActivity" android:exported="false" />

    </application>

</manifest>
