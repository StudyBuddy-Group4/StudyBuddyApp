<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="280dp"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_dialog"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Flag reason dialog -->
    <!-- Flag icon -->
    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:contentDescription="@string/flag_title"
        android:src="@drawable/ic_flag"
        app:tint="@color/red_prohibited" />

    <!-- Dialog title -->
    <!-- Asks why the participant is being flagged -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/flag_this_user_for"
        android:textColor="@color/dark_text"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Reason input -->
    <!-- Multi-line text box for the report reason -->
    <EditText
        android:id="@+id/etReason"
        style="@style/InputField"
        android:layout_marginTop="16dp"
        android:gravity="top|start"
        android:hint="@string/hint_write"
        android:importantForAutofill="no"
        android:inputType="textMultiLine"
        android:lines="3"
        android:minHeight="80dp" />

    <!-- Save button -->
    <!-- Submits the entered reason -->
    <Button
        android:id="@+id/btnSave"
        style="@style/PrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_button_primary"
        android:stateListAnimator="@null"
        android:text="@string/btn_save"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Cancel button -->
    <!-- Closes the dialog without reporting -->
    <Button
        android:id="@+id/btnCancel"
        style="@style/SecondaryButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_button_secondary"
        android:stateListAnimator="@null"
        android:text="@string/btn_cancel"
        android:textColor="@color/primary_green"
        android:textSize="16sp"
        android:textStyle="bold" />

</LinearLayout>
