<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary_green">

    <!-- Edit profile screen -->
    <!-- Landscape version -->
    <!-- Green header on top -->
    <!-- Landscape keeps the same form with wider side padding -->

    <!-- Top header background -->
    <!-- Green header area -->
    <View
        android:id="@+id/headerBackground"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_gradient_green"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.18"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Back action -->
    <!-- Returns to the profile screen -->
    <ImageView
        android:id="@+id/ivBack"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:contentDescription="@string/btn_back"
        android:src="@drawable/ic_arrow_back"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/white" />

    <!-- Screen title -->
    <!-- Main heading for the edit screen -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/edit_my_profile"
        android:textColor="@color/white"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/headerBackground"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/headerBackground" />

    <!-- Scrollable white card -->
    <!-- Holds the profile summary and edit form -->
    <!-- Lets the full form stay visible on shorter screens -->
    <ScrollView
        android:id="@+id/cardContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_card_white"
        android:fillViewport="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/headerBackground">

        <!-- Form content -->
        <!-- Wide side padding keeps the landscape form narrow -->
        <!-- Stacks the summary, fields, and action button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="250dp"
            android:paddingTop="24dp"
            android:paddingEnd="250dp"
            android:paddingBottom="24dp">

            <!-- Profile name in the card header -->
            <!-- Main identity line in the card header -->
            <TextView
                android:id="@+id/tvHeaderName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="16dp"
                android:text="@string/mock_user_name"
                android:textColor="@color/dark_text"
                android:textSize="24sp"
                android:textStyle="bold" />

            <!-- Profile id under the name -->
            <!-- Smaller line under the profile name -->
            <TextView
                android:id="@+id/tvHeaderId"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="4dp"
                android:text="@string/mock_user_id"
                android:textColor="@color/secondary_text"
                android:textSize="14sp" />

            <!-- Account settings section title -->
            <!-- Starts the editable part of the screen -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:text="@string/account_settings"
                android:textColor="@color/dark_text"
                android:textSize="18sp"
                android:textStyle="bold" />

            <!-- Username label -->
            <!-- Label for the username field -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:text="@string/username"
                style="@style/FieldLabel" />

            <!-- Username input -->
            <!-- Editable username field -->
            <EditText
                android:id="@+id/etUsername"
                style="@style/InputField"
                android:layout_marginTop="8dp"
                android:importantForAutofill="no"
                android:inputType="text"
                android:text="@string/mock_user_name" />

            <!-- Email label -->
            <!-- Label for the email field -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/email_address"
                style="@style/FieldLabel" />

            <!-- Email input -->
            <!-- Editable email field -->
            <EditText
                android:id="@+id/etEmail"
                style="@style/InputField"
                android:layout_marginTop="8dp"
                android:importantForAutofill="no"
                android:inputType="textEmailAddress"
                android:text="@string/hint_email" />

            <!-- Primary update action -->
            <!-- Main action for saving profile changes -->
            <Button
                android:id="@+id/btnUpdateProfile"
                style="@style/PrimaryButton"
                android:layout_width="match_parent"
                android:layout_marginTop="32dp"
                android:text="@string/btn_update_profile" />

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
